from functools import reduce
import operator

def sumar_llista(lst):
    return sum(lst)

def multiplicar_llista(lst):
    return reduce(operator.mul, lst, 1)

def main():
    print(sumar_llista([1, 2, 3, 4]))
    print(multiplicar_llista([1, 2, 3, 4]))

if __name__ == "__main__":
    main()